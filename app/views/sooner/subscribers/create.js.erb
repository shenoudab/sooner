<% if @subscriber.errors[:name].include?("is already taken") || @subscriber.errors[:email].include?("is already taken")%>
$('#subscriber').html("<%= escape_javascript(render(:partial => 'already_subscribed')) %>");
<% else %>
$('#subscriber').html("<%= escape_javascript(render(:partial => 'subscribed')) %>");
<% end %>